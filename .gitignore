# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.venv313/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data (personal health data + auto-generated DB - NEVER commit)
data/*.csv
data/*.json
data/*.db
!data/daily_log_template.csv
!data/web_checkin.html
!data/README.md

# Environment
.env
*.env.local
node_modules/
autonomic_age_dataset.csv

# WorkBuddy (local only — never commit)
.workbuddy/

# Pilot data + reports (local only, contains real user HRV)
data/pilot/
pilot_reports/

# Python test cache
tests/__pycache__/
.pytest_cache/
.coverage
htmlcov/

# 前端部署构建目录（由 data/web_checkin.html + src 引擎生成，不入库）
frontend_dist/

# 前端部署构建目录（push 到 gh-pages 用，不入库）
frontend_ghpages/
